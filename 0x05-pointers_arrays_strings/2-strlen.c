#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/**
 * strlen - count the lenght of a string
 * @s : string
 * Return: return the lenght of string
 */
int _strlen(char *s)
{

	int len;
	len = strlen(s);

	return (len);
}
