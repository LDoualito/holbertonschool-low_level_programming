#include <stdio.h>
#include <string.h>
/**
 *main - print stuff wihtout printf
 */
int main(void)
{
char str1[52];
strcpy(str1, "\"Programming is like building a multilingual puzzle");
puts(str1);
/**
 *Return: 1 if the text is good. 0 otherwise
 */
return (0);
}
